{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(?<slug>[^/]+)/(?<episode>[^/]+)",
      "dest": "/api/[slug]/[episode].js"
    }
  ]
}
